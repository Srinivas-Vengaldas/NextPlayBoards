# Copy to `apps/web/.env` for local dev. For Vercel, set these in the dashboard (same names).

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
# Browser calls API: production URL like https://your-app.vercel.app/api or `/api` when using Vercel same-origin
VITE_API_URL=
