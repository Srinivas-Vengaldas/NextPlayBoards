# Copy to `apps/web/.env` for local dev. For Vercel, set these in the dashboard (same names).

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
# Leave empty = same-origin `/api`. If set, must include `/api`, e.g. https://your-app.vercel.app/api (not site root only).
VITE_API_URL=
