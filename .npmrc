# Same effect as `pnpm install --frozen-lockfile` when the command is plain `pnpm install`.
# If Vercel's Install Command includes `--no-frozen-lockfile`, remove that flag so this setting applies.
frozen-lockfile=true
